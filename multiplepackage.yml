---
- hosts: all
  become: true
  tasks:
    - name: update apt cache and install Vim, Curl & Unzip packag
      apt:
         name: "{{items}}"
         update_cache: yes
      loop:                                
         - vim
         - curl
         - unzip

  
